import styles from "../styles/home.module.css";

const Home = () => {
  return (
    <div>
      <div className={styles.upperLeft}>
        <p>{`<Back`}</p>
      </div>
    </div>
  );
};

export default Home;
